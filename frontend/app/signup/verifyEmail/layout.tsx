import { pageMetadata } from "@/utils/metadata";
export const metadata = pageMetadata("Verify Your Email", "Verify your email address to continue joining Netflix Clone.", { social: true });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
