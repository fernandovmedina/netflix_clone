import { pageMetadata } from "@/utils/metadata";
export const metadata = pageMetadata("Set Up Your Account", "Create the credentials for your Netflix Clone membership.", { social: true });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
