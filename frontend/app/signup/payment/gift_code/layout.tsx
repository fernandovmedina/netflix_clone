import { pageMetadata } from "@/utils/metadata";
export const metadata = pageMetadata("Redeem a Gift Code", "Apply a gift or promotional code to your Netflix Clone plan.", { social: true });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
