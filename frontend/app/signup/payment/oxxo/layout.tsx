import { pageMetadata } from "@/utils/metadata";
export const metadata = pageMetadata("Pay with OXXO", "Create an OXXO payment reference for your Netflix Clone plan.", { social: true });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
