import { pageMetadata } from "@/utils/metadata";
export const metadata = pageMetadata("Choose a Plan", "Compare Netflix Clone plans and choose the one that fits you.", { social: true });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
