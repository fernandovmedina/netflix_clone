import { pageMetadata } from "@/utils/metadata";
export const metadata = pageMetadata("Registration Link Sent", "Check your email for a secure Netflix Clone registration link.", { social: true });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
